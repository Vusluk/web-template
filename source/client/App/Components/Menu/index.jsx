import React from 'react'
import classnames from 'classnames'
import style from './index.css'

const class

const Menu = ({
  view = {
    vertical: false,
    transparent: false,
  },
  actions = {},

  menu = [],
}) => {
  const classRoot = classnames(style.root, )
  return (
    <div className={style.root}>
    </div>
  )
}

export default Menu
